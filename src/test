//Пишем массив чисел от 1 до 10
        int [] numbers = new int[10];
        for (int i = 0; i < numbers.length; i++){
            numbers[i] = i + 1;
        }

        //Пишем массив чисел для римских
        String [] romeNum = new String[10];
        romeNum[0] = "I";
        romeNum[1] = "II";
        romeNum[2] = "III";
        romeNum[3] = "IV";
        romeNum[4] = "V";
        romeNum[5] = "VI";
        romeNum[6] = "VII";
        romeNum[7] = "VIII";
        romeNum[8] = "IX";
        romeNum[9] = "X";

        //Пишем массив арифместических символов
        char [] arChar = new char[4];
        arChar[0] = '-';
        arChar[1] = '+';
        arChar[2] = '*';
        arChar[3] = '/';

        //Запишем числа в переменные
        int a = Integer.parseInt(elements[0]);
        int b = Integer.parseInt(elements[2]);
        char c = elements[1];

        //Проверяем первую переменную

        for (int elem: numbers){
            if (a == elem){
                System.out.println("ok");
            }
        }

        for (int elem: numbers){
            if (b == elem){
                System.out.println("ok");
            }
        }


